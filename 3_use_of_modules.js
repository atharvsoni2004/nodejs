const now = require('./mydatetime');
const name = require ('./common_names')
console.log("here is object " + now.Date());
console.log("here is another object " + now.Time())
console.log("here is names are " + name.email);
console.log("here is names are " + name.name);